import styled from "styled-components";

const navColor = '#D3E4CD'

export const StyledNav = styled.nav`
    height: 150px;
    display: flex;
    flex-direction: column;
    background-color: ${navColor};
    border-radius: 1px;
    justify-content: center;
    
`